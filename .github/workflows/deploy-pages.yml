name: Deploy game to GitHub Pages
on:
  push:
    branches: ["main"]
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: pages
  cancel-in-progress: true
jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - run: |
          mkdir -p _site/assets
          cp site/index.html _site/index.html
          cp site/assets/index-DYzLEMuc.css _site/assets/
          touch _site/.nojekyll
          cat site/parts/index-CapWxO5a.js.gz.part-* > /tmp/game-bundle.js.gz
          gzip -dc /tmp/game-bundle.js.gz > _site/assets/index-CapWxO5a.js
          test "$(wc -c < _site/assets/index-CapWxO5a.js)" -eq 1628007
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site
      - id: deployment
        uses: actions/deploy-pages@v4
